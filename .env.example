# ─── Backend ──────────────────────────────────────────────────────────────────
PORT=5000
NODE_ENV=development

# MongoDB Atlas (producción / desarrollo directo)
MONGO_URI=mongodb+srv://<usuario>:<password>@cluster.mongodb.net/inventra?retryWrites=true&w=majority

# MongoDB local con Docker Compose
MONGO_USER=inventra_admin
MONGO_PASS=inventra_secret_2024
MONGO_DB=inventra_db

# JWT
JWT_SECRET=cambia_esto_por_un_secreto_largo_y_seguro
JWT_EXPIRE=7d

# CORS — URL del frontend en producción (sin / al final)
FRONTEND_URL=https://inventra-frontend.onrender.com
